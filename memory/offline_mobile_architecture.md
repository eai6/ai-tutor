---
name: Offline Mobile Tutoring Plan
description: Architectural decisions for native mobile app with offline tutoring via on-device LLMs (Gemma 3n / Qwen 3.5). Planning stage, no code yet.
type: project
originSessionId: 09ee43ad-944b-4bd2-a344-262985284940
---
# Offline Mobile Tutoring — Architecture Decisions (2026-04-23)

Native mobile app for the AI tutor with **offline tutoring via on-device small LLM**. Content generation stays online (Claude/Gemini server-side); only the live tutoring conversation runs on-device when offline. Driver: Seychelles pilot likely has intermittent connectivity.

## Core decisions

**On-device model candidates**: Gemma 3n E2B/E4B (native audio + vision, MediaPipe on Android, MLX on iOS, 128K ctx, 3.2GB / 5GB at 4-bit) OR Qwen 3.5 family — 0.8B / 2B / 4B / 9B variants — Apache 2.0, text+image+video (no audio), 262K ctx, llama.cpp + MLX. User wants multiple Qwen sizes available. No "Gemma 4" confirmed — stick with Gemma 3n.

**Why:** Different device tiers need different models; no single answer. Gemma 3n has audio (pedagogy unlock for voice answers), Qwen is smaller (reaches older phones) and Apache 2.0 (clean commercial bundling).

**How to apply:** Milestone 0 bake-off on math lesson (hardest case per `feedback_math_tutoring.md`) against all 4 candidates picks the v1 default, but architecture must stay pluggable.

---

**Shrink the on-device LLM's job** — today's engine makes 3–5 LLM calls/turn; offline shrinks to ≤2. Reply generation + answer classification via Gemma/Qwen; step advance, hint selection, remediation selection all become DETERMINISTIC rules against pre-baked lesson-pack data.

**Why:** 2–4B models can't reliably do the full pedagogical reasoning (remediation planning, coverage check, etc.). Making the state machine deterministic + using the small model only as the natural-language layer is the only realistic path.

**How to apply:** Any offline-mode engine work in the app follows this rule. If tempted to add a third LLM call per turn, reconsider.

---

**Policy-as-data, not ported logic.** Server emits the tutor state machine as JSON policy bundled with the lesson pack. App runs a generic state-machine runner. Do NOT port `conversational_tutor.py` logic to Dart — they will drift.

**Why:** One source of truth. Swap policies without shipping app updates. A/B test policies server-side.

**How to apply:** Milestone 2 is emitting the policy from existing Python state-machine code. App never hard-codes tutoring flow.

---

**Pluggable inference layer — mirror backend's `apps/llm/client.py`.** Abstract `InferenceClient` in the app (`generate(messages, systemPrompt)` → response) with implementations per runtime: `MediaPipeGemmaClient` (Android), `LlamaCppClient` (cross-platform), `MLXClient` (iOS). Server-side DB model `MobileInferenceModel` (mirrors backend's `ModelConfig`) — admin-configurable catalog: family, size, download_url, checksum, runtime, capabilities, chat_template, system_prompt_style, recommended_tier, quality_score. Endpoint: `GET /api/mobile/models`.

**Why:** Add new models (Qwen 4, Gemma 4, etc.) without an app update. Chat templates differ per family (Gemma `<start_of_turn>` vs Qwen `<|im_start|>`) — server-controlled template in catalog avoids hardcoding.

**How to apply:** Build the three layers (adapter, model manager, catalog) in Milestone 3. Ship v1 with 1 model exposed in UI, switcher hidden behind dev flag. Expose Model Store UI in v2 post-pilot.

---

**Lesson pack = existing step data + small additions.** `LessonStep` already carries `teacher_script`, `question`, `expected_answer`, `rubric`, `hint_1/2/3`, `answer_type`, `choices`, `educational_content` (worked examples, common mistakes, vocab, Seychelles context), `curriculum_context`. ~90% of what's needed for offline is already generated by `content_generator.py`.

NEW fields to add to offline bundle (small extensions to `content_generator.py`):
- `offline_remediation_variants[]` — 2–3 pre-written responses per `common_mistake` (today remediation is LLM-generated live)
- `answer_classifier_rubric` — structured form a 2–4B model can reliably use
- `step_system_prompts` — simplified per-step prompts tuned for on-device model

**Why:** Content-generation effort is small. Don't rebuild — assemble.

**How to apply:** Milestone 1 is `generate_offline_bundle()` in `content_generator.py` + `GET /api/lessons/<id>/offline-pack`.

---

**Three device tiers** based on RAM/storage detected at install:
- Flagship (iPhone 14+ / Pixel 7+ / equivalent) → E4B or Qwen 3.5 9B at 4-bit → full tutoring
- Mid-range (~3-yr-old phones) → E2B or Qwen 3.5 2B/4B at 4-bit → full tutoring
- Low-end → no on-device model → content-review mode only (read lesson, answer deterministic-graded MCQ exit ticket, no free-form chat)

**Graceful degradation inside a session:** low answer-classification confidence → show hint_1 rather than pretend to evaluate. Off-step questions → "I'll save that for when we're back online" + queue. Model errors 3× → fall back to content-review. Math: if model violates "never evaluate bare answers" rule (per `feedback_math_tutoring.md`), treat as low confidence and show `worked_example` verbatim.

---

**Offline-eligible step types** (proposed — confirm in Milestone 0):
- Eligible: `teach`, `worked_example`, `practice` with `answer_type` in {mcq, short_numeric, true_false}
- Deferred to online / fallback to worked example: `practice` with free-text, `data_interpretation`

---

## Sync boundaries

- Download: lesson pack (JSON policy + media zip) on WiFi by default, versioned
- Upload queue (append-only for most): `SessionTurn` rows, `engine_state` snapshots (last-write-wins per session), `ExitTicketAttempt`, flagged beyond-scope questions
- Teacher dashboard: offline sessions flagged with "offline" badge + per-turn `generated_offline=bool`
- Pack freshness: server keeps session's original pack version; if teacher edits a step mid-session, accept writes against old version and flag teacher
- Exit ticket offline allowed — teacher can manually override on review (teacher-override already exists per recent commits)

---

## Milestones (sequencing)

- **M0 — Feasibility + model bake-off** (1 week): prototype Gemma 3n E2B/E4B + Qwen 3.5 2B/4B on a phone against one math lesson. Gating risk — if no model handles math rules, project stops. Output feeds `quality_score` per model.
- **M1 — Offline bundle spec + API** (1 week, server only): `generate_offline_bundle()`, `GET /api/lessons/<id>/offline-pack`.
- **M2 — Policy-as-data state machine** (1–2 weeks, server only): emit JSON policy from existing Python engine; unit tests verify parity.
- **M3 — App shell** (3 weeks): Flutter, auth, course/lesson browser, pack download, local SQLite mirror. Content-review mode works. All 3 inference-layer abstractions built; one model wired up.
- **M4 — On-device LLM integration** (4 weeks — upped from 3 due to pluggability): MediaPipe Android + MLX iOS + llama.cpp fallback, state-machine runner, answer classifier, deterministic remediation picker.
- **M5 — Sync + teacher visibility** (2 weeks): queued writes, conflict handling, dashboard badges.
- **M6 — Pilot on real Seychelles devices** (ongoing).

Total to pilot-ready: ~12 weeks. M0 gates everything.

---

## Framework choice (locked 2026-04-23: React Native)

**React Native + Expo + TypeScript** chosen over Flutter. Rationale: solo developer, existing JS familiarity, larger community, `llama.rn` provides reasonable GGUF-based on-device LLM path without requiring Flutter's slightly-more-mature ML ecosystem. Main tradeoff accepted: Gemma 3n audio input path is weaker on iOS initially (no MediaPipe plugin via llama.rn) — deferred to v2.

**Execution details**: see `project_mobile_rn_plan.md` for concrete stack, project structure, phase sequencing, and API contract.

---

## Open risks to resolve

1. **Math-rule adherence on 2–4B models** — biggest unknown, gated in M0
2. **Pre-baked remediation feel** — may feel scripted; pilot-teacher feedback needed
3. **iOS review risk** — app store may scrutinize AI chat with minors; prepare content safety docs
4. **Device heterogeneity in schools** — biggest unknown for tier distribution

---

## Key backend files referenced (as of 2026-04-23, verify before editing)

- `apps/llm/client.py` — pattern to mirror for on-device inference abstraction
- `apps/curriculum/content_generator.py` — where `generate_offline_bundle()` will live
- `apps/tutoring/conversational_tutor.py` — where policy-as-data emitter will live
- `apps/curriculum/models.py::LessonStep` — step data shape (already offline-ready)
- `apps/tutoring/models.py::TutorSession, SessionTurn, ExitTicket, ExitTicketQuestion` — sync targets
